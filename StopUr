void StopUr() 
{
      switch(Index) {
    case 0:
      SwitchScreen(EncoderFunc());
      if (ButtonFunc("StopUr",1)){
        Index++;
        StopText = "Using Stop Ur";
        callStop = true;
      }
      break;
    case 1:

      if (ButtonFunc("StopUr",1)){
        Index = 0;
        StopText = "Stop Ur";
        callStop = true;
      }

      break;
    default:
      break;
  }

    if(callStop == true) 
  {
    int Index = 0;

    lcd.begin(16, 2);
    lcd.print(StopText);
    Serial.println(state);
    callStop = false;
    callGuess = true;
    callBoil = true;
    callAlarm = true;
    callClock = true;
    const int colorR = 0;
    const int colorG = 255;
    const int colorB = 0;
    lcd.setRGB(colorR, colorG, colorB);
  }
}
